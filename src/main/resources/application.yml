server:
  servlet:
    context-path: /api

spring:
  application:
    name: board

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379

  datasource:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true

jwt:
  issuer: open-my-app
  secret_key: open-my-256-bit-secret-key-here-hahaha
